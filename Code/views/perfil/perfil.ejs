<!DOCTYPE html>
<html lang="en">
<%-include('../Components/templateHead', {
        pageName:'Perfil', 
        cssFile: [
            '/css/Perfil/perfil.css',
        ]
    }) %>

<body>
    <script src="/scripts/Perfil/perfil.js"></script>

    <%-include('../Components/Header/headerComponent',{
            navigator:{
                label: ["Início", "Sobre Nós", "Artigos"],
                routes:[`/inicio`,`/sobrenos`,`/artigos/page1`]
            },
            loginSigin:{
                label: [user.name, "Logout"],
                routes: [``,`/usuarios/logout`]
            }
    
    }) %>

    <div  class= "Container" >
        <%- include('../Components/SideMenu/sideMenu', sideElement )%>
        <div id="rightContainer" style="display: flex; flex-direction: column; position: relative; width: 80%; margin-left: 250px;">
            <div class="container p-3 my-3 border " style="border:1px solid black; border-radius: 10px">          
                <section>
                <div class="mb-5"">
                    <h2 style="color: grey;">Informações Iniciais</h2>
                    <p>
                        <%=user.name%>, seja muito bem vindo ao Cash Book! Preencha o saldo inicial e comece a escrever e acompanhar sua história financeira!
                    </p>  

                </div>

                <form method="POST" action="/perfil" id="container">
                    <div class="form-group">
                      <label for="nome" class="ml-2">Nome</label>
                      <input type="text" class="form-control" id="nome" name="name" value="<%= user.name %> " placeholder="Nome">
                    </div>
                    <div class="form-group">
                        <label for="sobreNome" class="ml-2">Sobrenome</label>
                        <input type="text" class="form-control" id="sobrenome" name="surname" value="<%= user.surname %>" placeholder="Sobre nome">
                    </div>
                    <div class="form-group">
                        <label for="nome" class="ml-2">Saldo Inicial</label>
                        <input type="text" class="form-control" id="saldo" name="saldo" <% if(user.saldo != null){%> value="<%= user.saldo %> " <%}%>  " placeholder="Preencha o saldo inicial">
                    </div>

                    <button type="submit" class="btn btn-primary">Salvar dados</button>
                  </form> 
                </section>  
            </div>
        </div> 
    </div>    
            
    <%-include('../Components/footer') %>

</body>

</html>